[pytest]
# Default discovery: Phase 1 daily pipeline tests (run other suites via explicit paths, e.g. ``pytest oms/tests``).
testpaths = strategies/tests/factor_ls
norecursedirs = .git .venv venv node_modules __pycache__
