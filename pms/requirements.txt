# PMS (Phase 2: mark price, config; later: read/write loop)
# Use repo root venv; this file for Docker or isolated install.
pydantic>=2.0.0
pydantic-settings>=2.0.0
requests>=2.28.0
# Tests
pytest>=7.4.0
responses>=0.24.0
